<section class="about-section-bg min-h-screen px-6 py-16 text-white animate-fade-slide">
  <div class="max-w-7xl mx-auto space-y-16">

    <!-- Toast de sucesso -->
    {{#if sucesso}}
    <div id="toast"
      class="bg-green-100 text-green-800 border border-green-300 px-4 py-3 rounded relative max-w-md mx-auto animate-fade-slide">
      <strong class="font-bold">Sucesso:</strong>
      <span class="block sm:inline">Perfil atualizado com sucesso!</span>
      <button onclick="document.getElementById('toast').remove()" class="absolute top-1 right-2 text-xl">&times;</button>
    </div>
    {{/if}}

    <!-- Cabeçalho com boas-vindas -->
    <div class="flex justify-between items-center mb-12 flex-wrap gap-4">
      <div>
        <h1 class="text-4xl sm:text-5xl font-bold mb-2">Bem-vindo, {{user.nome}}!</h1>
        <p class="text-lg text-white/80">Veja suas estatísticas e acompanhe seu impacto 💙</p>
      </div>
      <a href="/logout"
         class="bg-red-500 hover:bg-red-600 text-white font-semibold px-6 py-2 rounded transition-all duration-300">
        Sair
      </a>
    </div>

    <!-- Cards de estatísticas -->
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 mb-16">
      <div class="bg-white/10 border border-white/20 rounded-lg p-6 text-center backdrop-blur-md animate-fade-slide delay-100">
        <h2 class="text-3xl font-bold">3</h2>
        <p class="mt-2 text-white/80">ONGs apoiadas</p>
      </div>
      <div class="bg-white/10 border border-white/20 rounded-lg p-6 text-center backdrop-blur-md animate-fade-slide delay-200">
        <h2 class="text-3xl font-bold">6</h2>
        <p class="mt-2 text-white/80">Doações realizadas</p>
      </div>
      <div class="bg-white/10 border border-white/20 rounded-lg p-6 text-center backdrop-blur-md animate-fade-slide delay-300">
        <h2 class="text-3xl font-bold">R$ 250,00</h2>
        <p class="mt-2 text-white/80">Total doado</p>
      </div>
    </div>

    <!-- Informações do usuário -->
    <div class="bg-white/10 border border-white/20 rounded-lg p-6 backdrop-blur-md animate-fade-slide delay-500">
      <h2 class="text-2xl font-semibold mb-4 text-white/90">Seus dados</h2>
      <ul class="text-white/80 space-y-2">
        <li><strong>Nome:</strong> {{user.nome}}</li>
        <li><strong>Email:</strong> {{user.email}}</li>
      </ul>
      <div class="mt-6">
        <a href="/perfil/editar"
           class="inline-flex items-center gap-2 border border-white text-white px-5 py-2 rounded hover:bg-white hover:text-[#2D4BA6] transition-all duration-300">
          ✏️ Editar perfil
        </a>
      </div>
    </div>

    <!-- Ações principais -->
    <div class="flex flex-col sm:flex-row justify-center gap-4 animate-fade-slide delay-700">
      <a href="/ongs"
         class="bg-white text-[#2D4BA6] font-semibold px-6 py-3 rounded hover:bg-gray-100 transition-all duration-300">
        Explorar ONGs
      </a>
    </div>
  </div>
</section>
